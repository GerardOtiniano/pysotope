[tool.poetry]
name = "osibl_correction"
version = "0.1.0"
description = ""
authors = ["Gerard Otiniano <gerardotiniano@gmail.com>"]
readme = "README.md"
packages = [{include = "OSIBL_correction", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
